pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'koyomi'

includeBuild 'plugin'

include 'annotation'
include 'core'
include 'logging'
