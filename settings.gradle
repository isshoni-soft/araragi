pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            url 'https://repo.isshoni.institute'
        }
    }
}

rootProject.name = 'koyomi'

includeBuild 'plugin'

include 'annotation'
include 'core'
include 'logging'
